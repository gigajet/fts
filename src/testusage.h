cout<<"test exec inp out cmp\n";
cout<<"Run exec < inp and compare output with out using cmp.\n";
cout<<"exec and cmp are executable files, inp and out are text files.\n";
cout<<"Emit no output if called with at least 3 arguments.\n";
cout<<"Exit code 0 if cmp return 0.\n";
cout<<"Exit code 1 if cmp return non-zero.\n";
cout<<"Exit code 2 with any other errors.\n";
